# 环境配置文件示例
# 复制此文件为 .env 并修改相应的值

# 服务器端口（默认 3000）
PORT=3000

# 服务器监听地址（生产环境使用 0.0.0.0，开发环境可用 localhost）
HOST=0.0.0.0

# Node 环境（development | production）
NODE_ENV=production

# 数据库路径（可选，默认为 ./data/poker.sqlite）
# DB_PATH=./data/poker.sqlite

# 日志级别（可选）
# LOG_LEVEL=info
